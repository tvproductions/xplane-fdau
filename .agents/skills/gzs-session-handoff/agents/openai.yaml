interface:
  display_name: "GovZero Session Handoff"
  short_description: "Preserve and resume engineering context"
  default_prompt: "Use $gzs-session-handoff to preserve the current engineering state for a fresh session."
policy:
  allow_implicit_invocation: false
