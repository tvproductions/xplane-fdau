# Task 1 Report: Distribution Rename and Native Package Relocation

## Implementation

- Updated the shared `origin` URL to `https://github.com/tvproductions/xplane-fdau.git`; no push, tag, release, or publication was performed.
- Renamed project metadata and console script to `xplane-fdau`, preserving version `0.1.0` and an empty runtime dependency list.
- Moved tracked native FDR sources with `git mv` to `xplane_fdau/formats/xplane_fdr/`; the native API remains complete at that temporary package boundary.
- Added the `xplane_fdau` root exporting only `__version__` and the `xplane_fdau.formats` namespace.
- Moved the CLI to `xplane_fdau.cli` with absolute native-FDR imports.
- Removed the former top-level package and generated bytecode directory so `find_spec("xplane_fdr")` is absent.
- Updated native test imports, artifact and smoke helpers, schema paths and identity, filename prefix, quality paths, and `uv.lock`.

## Preflight

Commands run:

```powershell
git status --short
git branch --show-current
git worktree list --porcelain
git tag --list
git remote -v
gh repo view tvproductions/xplane-fdau --json name,url,isPrivate,isArchived,latestRelease,issues,pullRequests
git remote set-url origin https://github.com/tvproductions/xplane-fdau.git
git remote get-url origin
```

Relevant output: clean worktree; branch `feature/xplane-fdr-core`; no tags; GitHub reported `xplane-fdau`, public, unarchived, with `latestRelease: null`; final remote URL was exactly `https://github.com/tvproductions/xplane-fdau.git`.

## TDD Evidence

The tests added first protect the new distribution metadata, root-only export surface, absent legacy namespace, and relocated native public API.

RED command:

```powershell
uv run python -m unittest tests.test_project_metadata tests.test_public_api -v
```

Relevant RED output:

```text
FAIL: expected 'xplane-fdau' != 'xplane-fdr'
ERROR: ModuleNotFoundError: No module named 'xplane_fdau'
FAIL: ModuleSpec(name='xplane_fdr', ...) is not None
FAILED (failures=2, errors=2)
```

GREEN command:

```powershell
uv run python -m unittest tests.test_project_metadata tests.test_public_api -v
```

Relevant GREEN output:

```text
Ran 4 tests in 0.001s
OK
```

## Verification

Commands run:

```powershell
uv run python -m unittest discover -v
uv run ruff check xplane_fdau tests tools
uv run ruff format --check xplane_fdau tests tools
uv run ty check
git diff --check
```

Relevant output:

```text
Ran 175 tests in 0.740s
OK
All checks passed!
35 files already formatted
All checks passed!
```

## Files Changed

- `pyproject.toml` and `uv.lock`
- `xplane_fdau/__init__.py`, `xplane_fdau/formats/__init__.py`, and relocated modules/resources under `xplane_fdau/formats/xplane_fdr/`
- `xplane_fdau/cli.py`, the mirrored schema under `docs/schemas/`, all native FDR tests, and release/smoke/quality tools

## Self-Review

- No tracked `xplane_fdr` path remains.
- `xplane_fdau.__all__` is exactly `["__version__"]`; native public names are available at `xplane_fdau.formats.xplane_fdr`.
- Moved runtime modules retain their internal relative imports; the relocated CLI uses the required absolute native imports.
- Packaged and published schemas have matching `xplane-fdau` identities.
- Legacy command/import surfaces are absent from active package metadata; the new console command is covered by the CLI test.
- Independent review initially observed an incomplete index during the move phase. Direct post-review inspection confirmed the final index contains the coupled metadata, source, tests, tools, schema, and report files with no unstaged differences.

## Concerns

- Task 1 intentionally leaves `FDRRecordingSession` and other recording/sink names in `xplane_fdau.formats.xplane_fdr`; Task 2 owns their separation.
- Task 1 intentionally leaves the existing flat native CLI grammar; Task 3 owns `xplane-fdau fdr ...`.
- Review observations about removing release-tag validation, changing the fuller schema title, and migrating CI/pre-commit release surfaces are intentionally deferred to their scheduled Task 4/5 scope; they are not part of this atomic Task 1 brief.
- `uv run` emits an existing build-system constraint warning because `uv_build>=0.11.26,<0.12` excludes installed uv 0.12.1; all required gates exit zero.
