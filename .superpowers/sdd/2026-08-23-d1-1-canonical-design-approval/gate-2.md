# Verification Evidence

- **Child:** `D1.1`
- **Gate:** `2`
- **Kind:** verification
- **Result:** passed
- **Date:** 2026-08-23
- **Subject:** Exact and versioned canonical contract decisions

The approved design fixes every D1.1 contract-decision family in named
normative sections:

- contract families, schema IDs/versioning, canonical JSON, binary64/integer
  lexical rules, UTF-8/LF bytes, SHA-256, and self-hash preimages;
- semantic/runtime identity, authority, provenance, immutable references,
  inline typed values, enumeration domains, and content-addressed payloads;
- measurement definitions/catalogs, source bindings/catalogs, transform DAGs,
  payload compatibility, applicability, and failure dispositions;
- clock domains/readings, UTC instants, exact anchors, observation timing,
  raw observations, normalized samples, validity/quality, freshness/staleness,
  lineage/derivation, and deterministic frame closure;
- public modules, exports and signatures, exact error precedence and JSON
  Pointers, Draft 2020-12 schema resources, fixture/corpus layout and manifest;
  and
- Python/native conformance-result shapes, case classification, canonical-byte
  parity, and runner exit outcomes.

The design uses closed vocabularies, exact property inventories, deterministic
ordering and validation precedence, immutable revision/hash references, and
explicit future-contract boundaries. No implementation choice remains for the
version-1 wire and semantic contracts within D1.1 scope.
